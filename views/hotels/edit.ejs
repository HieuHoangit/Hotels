<!DOCTYPE html>
<html>
<head>
    <title>Chỉnh sửa Hotel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Chỉnh sửa Hotel</h1>
        <form action="/hotels/edit/<%= hotel.id %>" method="POST">
            <div class="mb-3">
                <label for="name" class="form-label">Tên Hotel</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= hotel.name %>" required>
            </div>
            <div class="mb-3">
                <label for="price" class="form-label">Giá</label>
                <input type="number" step="0.01" class="form-control" id="price" name="price" value="<%= hotel.price %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Cập nhật</button>
        </form>
    </div>
</body>
</html>
